---
subcategory: "Advanced Bot Protection"
layout: "incapsula"
page_title: "incapsula_abp_pending_changes"
description: |-
  Exposes a hash to facilitate change detection caused by e.g. a depends_on = [module.x]
---


# incapsula_abp_pending_changes

Exposes a hash to facilitate change detection caused by e.g. a depends_on = [module.x]

## Example Usage

```terraform
# Returns the hash of the account's pending (unpublished) configuration. Make it
# depend on the ABP resources you want included in the snapshot so the hash is
# recomputed whenever any of them change.
data "incapsula_abp_pending_changes" "current" {
  depends_on = [module.abp]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `hash` (String) Stable (but computed) marker facilitating change-detection if any resource that this data depends on changes
- `id` (String) The ID of this resource.
