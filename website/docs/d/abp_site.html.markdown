---
subcategory: "Advanced Bot Protection"
layout: "incapsula"
page_title: "incapsula_abp_site"
description: |-
  Looks up an existing ABP Site (a.k.a. Website Group) within an account by its site_id, or (XOR) its name. Exactly one of site_id or name must be set. Use this to reference a Site that is not managed by this Terraform configuration, for example when building an incapsula_abp_account_site_priority list. The lookup fails if zero or more than one Site matches.
---


# incapsula_abp_site

Looks up an existing ABP Site (a.k.a. Website Group) within an account by its `site_id`, or (XOR) its `name`. Exactly one of `site_id` or `name` must be set. Use this to reference a Site that is not managed by this Terraform configuration, for example when building an `incapsula_abp_account_site_priority` list. The lookup fails if zero or more than one Site matches.

## Example Usage

```terraform
# Look up a Site that is not managed by this Terraform configuration, e.g. when
# building an `incapsula_abp_account_site_priority` list. Provide exactly one of
# `site_id` or `name`.
data "incapsula_abp_site" "external_site" {
  account_id = var.account_id
  name       = "Externally managed site"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_id` (String) ABP account UUID to search within.

### Optional

- `name` (String) Name of the Site to look up. Matched exactly and case-sensitively. One of (but not both) `site_id` or `name` is required.
- `site_id` (String) ID of the Site to look up. One of (but not both) `site_id` or `name` is required.

### Read-Only

- `created_at` (String) RFC3339 timestamp at which the Site was created.
- `default_max_requests_per_minute` (Number) Default maximum number of requests without a token per minute.
- `default_max_requests_per_session` (Number) Default maximum number of requests without a token per session.
- `default_max_session_length` (String) Default maximum length of a session without a token, in moi duration format.
- `default_selector` (List of Object) Catch-all selector matching all request paths after all other selectors. (see [below for nested schema](#nestedatt--default_selector))
- `id` (String) The ID of this resource.
- `modified_at` (String) RFC3339 timestamp at which the Site was last modified.
- `selector` (List of Object) Ordered list of user-defined Selectors for this Site. (see [below for nested schema](#nestedatt--selector))

<a id="nestedatt--default_selector"></a>
### Nested Schema for `default_selector`

Read-Only:

- `analysis_settings` (String)
- `id` (String)
- `path_prefix` (String)
- `policy_id` (String)


<a id="nestedatt--selector"></a>
### Nested Schema for `selector`

Read-Only:

- `analysis_settings` (String)
- `id` (String)
- `path_prefix` (String)
- `path_regex` (String)
- `policy_id` (String)
- `postback` (String)
